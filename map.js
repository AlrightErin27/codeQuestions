// const numbers = [12, 39, 0, 13, 6, 5];

// const timesTwo = numbers.map((number, idx) => {
//   //for ARRAYS ONLY
//   //Whatever you return is going to be in the same index
//   //as the mirror world version of it
//   //creates new array
//   return number * 2;
//   //if return idx you get the index array [0, 1, 2, 3, 4, 5. 6]
// });
// console.log(timesTwo);

const timesTwo = numbers.map((number, idx) => {
  //for ARRAYS ONLY
  //Whatever you return is going to be in the same index
  //as the mirror world version of it
  //creates new array
  return number * 2;
  //if return idx you get the index array [0, 1, 2, 3, 4, 5. 6]
});
//console.log(timesTwo);
//--------------------------------------------------------
